{
  "name": "cityzen-sandbox-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0",
    "typescript": "~6.0.2"
  }
}
